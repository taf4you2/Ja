<UserControl x:Class="Ja.Views.CalendarView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="900" d:DesignWidth="1400">

    <Grid Background="#F5F5F5">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <Border Grid.Row="0" Background="White" Padding="20" Margin="0,0,0,20">
            <Grid>
                <StackPanel Orientation="Horizontal">
                    <TextBlock Text="📅 Kalendarz" FontSize="24" FontWeight="Bold"/>
                </StackPanel>
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                    <Button Content="+ Dodaj trening" Background="#1976D2" Foreground="White"
                            Padding="15,8" BorderThickness="0" FontWeight="SemiBold"/>
                </StackPanel>
            </Grid>
        </Border>

        <!-- Month Navigation -->
        <Border Grid.Row="1" Background="White" Padding="20" Margin="0,0,0,20">
            <Grid>
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                    <Button Content="◀" Width="40" Height="40" Margin="0,0,20,0"/>
                    <TextBlock Text="Listopad 2025" FontSize="20" FontWeight="SemiBold" VerticalAlignment="Center"/>
                    <Button Content="▶" Width="40" Height="40" Margin="20,0,0,0"/>
                    <Button Content="Dzisiaj" Padding="15,8" Margin="20,0,0,0"/>
                </StackPanel>

                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                    <Border Background="#E3F2FD" CornerRadius="4" Padding="12,6" Margin="0,0,10,0" Cursor="Hand">
                        <TextBlock Text="Miesiąc" FontWeight="SemiBold" Foreground="#1976D2"/>
                    </Border>
                    <Border Background="Transparent" BorderBrush="#E0E0E0" BorderThickness="1"
                            CornerRadius="4" Padding="12,6" Margin="0,0,10,0" Cursor="Hand">
                        <TextBlock Text="Tydzień" Foreground="#757575"/>
                    </Border>
                    <Border Background="Transparent" BorderBrush="#E0E0E0" BorderThickness="1"
                            CornerRadius="4" Padding="12,6" Cursor="Hand">
                        <TextBlock Text="Lista" Foreground="#757575"/>
                    </Border>
                </StackPanel>
            </Grid>
        </Border>

        <!-- Calendar Grid -->
        <Border Grid.Row="2" Background="White" Padding="20">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <!-- Day Headers -->
                <Grid Grid.Row="0" Margin="0,0,0,10">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>

                    <TextBlock Grid.Column="0" Text="Poniedziałek" HorizontalAlignment="Center" FontWeight="SemiBold"/>
                    <TextBlock Grid.Column="1" Text="Wtorek" HorizontalAlignment="Center" FontWeight="SemiBold"/>
                    <TextBlock Grid.Column="2" Text="Środa" HorizontalAlignment="Center" FontWeight="SemiBold"/>
                    <TextBlock Grid.Column="3" Text="Czwartek" HorizontalAlignment="Center" FontWeight="SemiBold"/>
                    <TextBlock Grid.Column="4" Text="Piątek" HorizontalAlignment="Center" FontWeight="SemiBold"/>
                    <TextBlock Grid.Column="5" Text="Sobota" HorizontalAlignment="Center" FontWeight="SemiBold"/>
                    <TextBlock Grid.Column="6" Text="Niedziela" HorizontalAlignment="Center" FontWeight="SemiBold"/>
                </Grid>

                <!-- Calendar Days Grid -->
                <Grid Grid.Row="1">
                    <TextBlock Text="[Kalendarz - siatka dni w budowie]"
                               FontSize="16" Foreground="#9E9E9E"
                               HorizontalAlignment="Center" VerticalAlignment="Center"/>
                </Grid>
            </Grid>
        </Border>
    </Grid>
</UserControl>
